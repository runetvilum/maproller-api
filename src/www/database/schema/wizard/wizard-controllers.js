(function (window, angular, console, tv4) {
    'use strict';
    angular.module('myApp.controllers').controller('schema-wizard', ['$scope', '$http',
        function ($scope, $http) {
            
        }
    ]);
})(this, this.angular, this.console, this.tv4);