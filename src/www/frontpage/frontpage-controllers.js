(function (window, angular, console) {
    'use strict';
    angular.module('myApp.controllers').controller('frontpage', ['$scope',
        function ($scope) {
            
        }
    ]);
})(this, this.angular, this.console);