(function (window, angular, console) {
    'use strict';
    angular.module('myApp.controllers').controller('home', ['$scope', '$rootScope', '$http', 'profile',
        function ($scope, $rootScope, $http, profile) {
            
        }
    ]);
})(this, this.angular, this.console);