(function (window, angular, console) {
    'use strict';
    angular.module('myApp.controllers').controller('organization-databases', ['$scope', '$rootScope', '$http', '$stateParams', 'md5',
        function ($scope, $rootScope, $http, $stateParams, md5) {
            
            
        }]);
})(this, this.angular, this.console);