(function (window, angular, console) {
    'use strict';
    angular.module('myApp.controllers').controller('templates', ['$scope', '$rootScope', '$http',
        function ($scope, $rootScope, $http) {
            
        }
    ]);
})(this, this.angular, this.console);